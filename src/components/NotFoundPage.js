import React from 'react';

const NotFoundPage = () => {
    return (
        <div>
            <p>Oops! Looks like you found a 404 instead of a page!</p>
        </div>
    );
};

export default NotFoundPage;